<Window x:Class="Store.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Store"
        xmlns:viewmodel="clr-namespace:Store.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Управление Маркетплейсом" FontSize="30" HorizontalAlignment="Left" 
                   Margin="196,10,0,0" VerticalAlignment="Top" Width="412" Height="50" 
                   Grid.ColumnSpan="3" />
        <Button Grid.Column="1" Content="Вход" HorizontalAlignment="Left" Margin="60,10,0,0" 
                    Grid.Row="1" VerticalAlignment="Top" Width="150" Height="50" FontSize="16"
                    Command="{Binding LogNavCommand}"/> <!--Click="Log_Click"-->

        </Grid>
</Window>
